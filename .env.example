# URL base del backend/webhook de citas (dejar vacío mientras no exista backend público).
# Cuando exista, apunta a algo como: https://api.tudominio.com
# El código de src/services/appointmentService.js usará esta variable automáticamente.
VITE_API_BASE_URL=
